# Twenty-five additional synchronous-loop sites

Manual source review on 2026-09-16, limited to the 25 sites listed below. The prior loop reviews and `README.md` were read first; these sites are additional to their enumerated reviews. `additional-sites.json` captures each exact source path, loop line, and SHA-256. This is source-level progress and lifetime evidence, not an incident attribution, heap measurement, or a claim that every remaining loop has been reviewed.

| Site                                                            | Progress, retained state, and caller/cleanup review                                                                                                                                                                                                                                                                                                                                                                                                                                     | Verdict                                                                                                                           |
| --------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| `src/main/text-generation/pull-request-context.ts:61`           | Reverse generator decrements `index` for ordinary characters and moves before a CR/LF delimiter otherwise. The error summarizer stops at the first nonempty yielded line. No accumulating collection in the loop; git context calls request a 10 MiB subprocess max buffer.                                                                                                                                                                                                             | Finite input scan.                                                                                                                |
| `src/main/gitlab/mr-file-diffs.ts:29`                           | `countDiffLines` advances to newline + 1 or breaks. Holds counters, not a line array. `fetchMRFiles` maps one API page of at most 100 requested files, retaining each requested diff in its returned result.                                                                                                                                                                                                                                                                            | Finite scan; input diff bytes still determine output size.                                                                        |
| `src/main/crash-reporting/process-gone-dedupe.ts:69`            | Prune deletes the first actual Map key each iteration or breaks on empty. Default process singleton has a 128-key cap plus a two-second expiry checked on claims. `release` deletes only its own claim identity.                                                                                                                                                                                                                                                                        | Bounded state; no growing iteration.                                                                                              |
| `src/main/crash-reporting/gpu-crash-fallback-decision.ts:63`    | `stale` increases to array length; one splice removes that prefix. Real singleton uses 30 seconds/threshold 3; engagement suppresses later pushes. `gpu-lifecycle.ts` can disengage after withholding fallback, so threshold 3 is not an unconditional array-size cap. Monotonic normalization and rolling expiry remain; entries are numbers.                                                                                                                                          | Finite scan. No input-independent count bound claimed across arbitrary disengage sequences.                                       |
| `src/main/gitlab/gitlab-known-host-probe.ts:84`                 | Unauthenticated-host prune deletes one oldest key or returns, giving 128 entries. Adjacent successful known-host map is different: execution keys contain SSH generations and old successful entries are never removed on reconnect. Production reset exists in `preflight/agent-detection.ts:359`, only for forced preflight. `ssh-git-dispatch.ts` increments generations on register/unregister and does not invalidate this map.                                                    | Selected loop bounded. Obsolete successful-generation retention remains a concrete follow-up candidate, not a measured leak here. |
| `src/main/gitlab/gitlab-auth-and-rate-limit.ts:139`             | TTL pass only deletes existing entries; count pass deletes oldest or breaks. Remember prunes before and after insert, leaving at most 64 snapshots of small scalar fields. `getRateLimit` always releases GitLab admission in finally.                                                                                                                                                                                                                                                  | Bounded cache.                                                                                                                    |
| `src/main/crash-reporting/minidump-crash-signature.ts:145`      | `end` increases until printable bytes stop or at most 4,000 bytes past candidate start. Outer marker search advances and examines at most 256 markers per four severities. Only successful decoded strings escape; no Buffer view escapes into signature. The separately audited crashpad file reader owns dump acquisition.                                                                                                                                                            | Finite bounded candidate scan; whole dump search cost remains input-sized.                                                        |
| `src/main/crash-reporting/minidump-crashpad-annotations.ts:129` | Each trailing-NUL removal strictly shortens the string. `MinidumpView.byteArray` caps annotation value bytes at 8,192; annotation count <= 512, module list <= 1,024, keys pass a fixed allowlist. Slices may retain a small bounded original string, not the dump Buffer.                                                                                                                                                                                                              | Bounded parsing; no persistent parser collection.                                                                                 |
| `src/main/crash-reporting/crash-breadcrumb-store.ts:77`         | Retained breadcrumb insertion removes oldest entries until <= 8. Record sanitization occurs before storing. Recent ring separately holds <= 30 entries.                                                                                                                                                                                                                                                                                                                                 | Bounded retained lane.                                                                                                            |
| `src/main/crash-reporting/crash-breadcrumb-store.ts:227`        | Overflow preserves the oldest coalesced entry's pending evidence then deletes that actual key, ending at 128. Preservation calls ordinary record insertion, which does not grow the coalesce Map. Pending payload replaces prior payload.                                                                                                                                                                                                                                               | Bounded key count; pending unsanitized payload size is caller-dependent.                                                          |
| `src/main/crash-reporting/crash-breadcrumb-store.ts:282`        | `visibleReportWindow` copies a <= 30-entry visible ring and splices one existing element each iteration. Its sole callers compute budget as 30 minus retained-visible count <= 8, so budget >= 22; the negative-budget infinite-empty-loop case is unreachable from these callers.                                                                                                                                                                                                      | Bounded loop under actual caller invariant.                                                                                       |
| `src/main/rate-limits/opencode-go-page-scraper.ts:23`           | Global regex always consumes at least a key and colon; repeated calls advance `lastIndex`. Inner brace and numeric scans increment indexes. Public parser refuses strings over 10,000,000 characters. Repeated malformed key/brace candidates can rescan large suffixes, so this is not a linear-time claim. `opencode-go-usage-fetcher.ts` materializes `Response.text()` before this check.                                                                                           | Finite parsing with potential superlinear work; pre-parse body acquisition is not capped by parser check.                         |
| `src/main/integration-credential-file.ts:56`                    | Successful write must return positive bytes, otherwise throws; `written` strictly increases toward existing Buffer length. Success fsyncs/closes/renames; catch closes any owned descriptor, tries to unlink temporary file, and rethrows. No per-write collection.                                                                                                                                                                                                                     | Finite progress and owned descriptor cleanup.                                                                                     |
| `src/main/crash-reporting/self-initiated-tree-kill-log.ts:129`  | Overflow helper splices exactly one element at an index >= 0 and below the nonempty array's length. Post-insert ring <= 32; read/filter/detail formatting operate on that bound. Site/scope values originate at instrumented process-kill callers.                                                                                                                                                                                                                                      | Bounded ring.                                                                                                                     |
| `src/main/gitlab/gitlab-project-ref-resolution.ts:61`           | Positive/negative insertion deletes oldest keys until <= 512; runtime generation and known-host list are part of key. Coalesced producer checks `ownsKey` before publication. Negative expiry checked on lookup; positive stale generations still cannot exceed entry cap.                                                                                                                                                                                                              | Bounded count; key/value string bytes input-dependent.                                                                            |
| `src/shared/secure-path-hardening-cache.ts:53`                  | Rejects individually oversized keys before eviction; each loop deletes oldest and subtracts its byte accounting, or returns if empty. Actual production consumers use finite shared defaults: 1,024 entries, 64 KiB/key, 512 KiB aggregate key bytes. Values are booleans or fixed scalar stat/retry records.                                                                                                                                                                           | Bounded production cache; arbitrary invalid constructor bounds not assumed safe.                                                  |
| `src/shared/terminal-retirement-proof-ledger.ts:39`             | Local Map built from existing/input proofs; overflow deletes oldest string key until <= 64. Runtime stores the returned bounded array; web reader uses same append routine and drops proofs for republished live surfaces. No mutation of input collection while iterating it.                                                                                                                                                                                                          | Bounded retained result; temporary Map/input sizes depend on supplied arrays.                                                     |
| `src/main/git/remote-name-listing.ts:56`                        | TTL deletion followed by oldest-key deletion; called before lookup and after successful owned probe insertion. At most 512 repo/runtime entries, with 30-second/5-minute TTL. Coalesced probes remove only their own slot in finally; subprocess requests have a timeout. Remote-name arrays remain sized by repository input.                                                                                                                                                          | Bounded cache entries, no growing loop.                                                                                           |
| `src/shared/quick-open-listing-limits.ts:102`                   | Every delimiter moves `cursor` +1; absent delimiter returns. Partial field copied into `GrowingByteBuffer`, rejected/cleared over 64 KiB, so tiny suffix cannot pin a large incoming buffer. `take`/`clear` replace backing storage. `filesystem-search-file-paths.ts` clears on abort/deadline and removes child/signal listeners on finish. That caller ignores `path-too-large`, a correctness follow-up rather than an unbounded accumulator.                                       | Bounded partial-field retention.                                                                                                  |
| `src/shared/quick-open-expansion-paths.ts:16`                   | Slash search starts strictly after previous slash or breaks when ancestor found. Temporary sorted list and output Map are local to call; output has no more entries than input. `quick-open-readdir-walk.ts` consumes collapsed paths for the next scan frontier.                                                                                                                                                                                                                       | Finite input-sized transform.                                                                                                     |
| `src/main/git/local-repo-ref-maintenance.ts:43`                 | Each overflow deletes oldest Map key; at most 64 closures. `disposeLocalRepoRefMaintenance` disposes maintenance timers, clears all probes, drops singleton, and awaits an active packed-refs lock rewrite rather than killing it. Runtime producer replaces a repo key's callback.                                                                                                                                                                                                     | Bounded callback registry with app-lifetime cleanup.                                                                              |
| `src/main/rate-limits/codex-rpc-rate-limit-probe.ts:206`        | Each iteration removes through newline before parse/continue. Settling removes stdout/stderr/error/close/abort listeners and timer. Stdin error observer intentionally lasts until child close. Production initial/RPC deadlines are 30/10 seconds or WSL 40/25; termination adds bounded waits. Unterminated `buffer` has no byte cap; repeated matching initialization replies can rearm the deadline. No ordinary large/repeated-init Codex producer was established in this review. | Finite loop; unbounded incomplete-frame bytes remain a conditional input risk.                                                    |
| `src/shared/git-status-porcelain-parser.ts:56`                  | Each parsed line advances `start`, or no newline breaks. Main and relay use `resolveGitStatusLimit` then stop child when callback reports excess. Changed/unmerged entries count; ignored paths do not, and partial line has no byte cap. `--ignored=matching` is only enabled on explicit includeIgnored paths. Ignored output is semantically requested, so adding an arbitrary cap would alter behavior.                                                                             | Finite scan; requested ignored-path collection and partial line remain input-sized.                                               |
| `src/shared/agent-hook-spool.ts:72`                             | Each newline advances `start`/`consumed`; no newline ends parsing. Drain handles at most 1,024 nonempty files sequentially and closes `r+` descriptor in finally. POSIX writer checks preexisting file size < 5 MiB before append, but reader `readFileSync`, concurrent append, final record, and unread-tail allocation do not enforce that as a hard byte cap. Directory names are enumerated before candidate slicing.                                                              | Finite parsing; whole-file/tail memory bound not proven by writer precheck.                                                       |
| `src/shared/resolved-worktree-lineage.ts:59`                    | Each path records current ID; encountering same ID breaks, then path nodes enter global processed set. Every lookup moves to parent. Caller prefilters valid same-boundary/incarnation edges; Maps/Sets are local and returned projections are tied to supplied worktree collection.                                                                                                                                                                                                    | Finite cycle-aware traversal, O(input nodes) auxiliary retention.                                                                 |

## Follow-up priority

The selected loops do not expose a growing synchronous collection iterator. Three separate surrounding lifetime/byte issues remain for bounded evidence work: obsolete GitLab successful execution generations, incomplete Codex RPC stdout, and spool whole-file acquisition. These observations are not explanations of #19831 or #19768. A fourth lead supplied by the parent is repeated timed-out `createAuthFilesystemOperation.wait()` races on one unresolved native operation; it is outside these 25 sites and will be tested separately before any fix proposal.
